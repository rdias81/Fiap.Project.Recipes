@page
@model Fiap.Project.Recipes.Web.Views.Categoria.IndexModel
@{ Layout = null; }
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        <a asp-page="Incluir">Criar Novo Registro</a>
    </p>
    <table>
        <tr>
            <th>Id</th>
            <th>Descrição</th>
            <th>...</th>
        </tr>
        @foreach (var item in Model.Categorias)
        {
    <tr>
        <td>@item.Id</td>
        <td>@item.Titulo</td>
        <td>@item.Descricao</td>        
        <td>
            <a asp-page="Editar" asp-route-id="@item.Id">Alterar Registro</a> |            
            <a asp-page="Excluir" asp-route-id="@item.Id">Excluir</a>
        </td>
    </tr>
        }
    </table>
</body>
</html>
